<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fresh.core.mapper.OrderItemMapper">
    
    <insert id="insertBatch">
        INSERT INTO order_items (
            order_id, product_id, product_name, product_image,
            price, quantity, create_time, update_time
        ) VALUES
        <foreach collection="items" item="item" separator=",">
            (
                #{item.orderId}, #{item.productId}, #{item.productName},
                #{item.productImage}, #{item.price}, #{item.quantity},
                NOW(), NOW()
            )
        </foreach>
    </insert>
    
    <select id="selectByOrderId" resultType="com.fresh.core.entity.OrderItem">
        SELECT * FROM order_items
        WHERE order_id = #{orderId}
        ORDER BY create_time ASC
    </select>
    
</mapper>